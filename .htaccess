RewriteEngine On

# Захватываем всё после /api и передаём в api.php как параметр 'path'
RewriteCond %{REQUEST_URI} ^/api/(.*)$
RewriteRule ^api/(.*)$ api.php?path=$1 [QSA,L]

# Остальные правила для вашего сайта (если есть) остаются без изменений
RewriteRule ^admin$ admin.php [L]
RewriteRule ^lessons$ lessons.php [L]
RewriteRule ^login$ login.php [L]
RewriteRule ^logout$ logout.php [L]
RewriteRule ^signup$ signup.php [L]
RewriteRule ^profile$ profile.php [L]
